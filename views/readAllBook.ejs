<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Books</title>

    <!-- Bootstrap CSS from CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Custom CSS for dark theme -->
    <style>
        body {
            background-color: #212529; /* Dark background for the body */
            color: #e9ecef; /* Light text color */
        }
        .book-list-container {
            background-color: #343a40; /* Dark background for the container */
            padding: 2rem;
            border-radius: 0.5rem;
            max-width: 900px;
            margin: 2rem auto;
        }
        .book-list-container h1 {
            margin-bottom: 1.5rem;
            color: #e9ecef; /* Light text color for the title */
        }
        .book-list {
            list-style: none;
            padding: 0;
        }
        .book-list li {
            background-color: #495057; /* Dark background for each book item */
            margin-bottom: 1rem;
            padding: 1rem;
            border-radius: 0.5rem;
            transition: background-color 0.5s ease, transform 0.7s ease;
        }
        .book-list li:hover {
            background-color: #6c757d; /* Slightly lighter background on hover */
            background-color: #6c757d; /* Slightly lighter background on hover */
            transform: scale(1.07); /* Slightly increase size on hover */
        }
        .book-list h2 {
            margin-bottom: 0.5rem;
        }
        .book-list p {
            margin-bottom: 0.5rem;
        }
        .btn-primary {
            background-color: #007bff; /* Primary button background color */
            border-color: #007bff; /* Primary button border color */
            text-decoration: none; /* Remove underline from links */
        }
        .btn-primary:hover {
            background-color: #0056b3; /* Darker shade of primary color on hover */
            border-color: #004080; /* Darker border color on hover */
        }
        .btn-back {
            background-color: #6c757d; /* Secondary button background color */
            border-color: #6c757d; /* Secondary button border color */
            text-decoration: none; /* Remove underline from links */
        }
        .btn-back:hover {
            background-color: #5a6268; /* Darker shade of secondary color on hover */
            border-color: #4e555b; /* Darker border color on hover */
        }


    </style>
</head>

<body>
    <div class="book-list-container">
        <h1>All Books</h1>

        <% if (books && books.length > 0) { %>
            <ul class="book-list">
                <% books.forEach(book => { %>
                    <li class>
                        <h2><%= book.title %></h2>
                        <p><strong>Author:</strong> <%= book.author %></p>
                        <p><strong>Published:</strong> <%= book.published_date %></p>
                        <a href="/library/read-book/<%= book.id %>" class="btn btn-primary">View Details</a>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>No books available.</p>
        <% } %>

        <a href="/library/read-book" class="btn btn-back">Back to Library</a>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>
