<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Book</title>

    <!-- Bootstrap CSS from CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Custom CSS for dark theme -->
    <style>
        body {
            background-color: #212529; /* Dark background for the body */
            color: #e9ecef; /* Light text color */
        }
        .container {
            background-color: #343a40; /* Dark background for the container */
            padding: 2rem;
            border-radius: 0.5rem;
        }
        .form-control {
            background-color: #495057; /* Dark background for input fields */
            color: #e9ecef; /* Light text color in input fields */
            border-color: #6c757d; /* Border color for input fields */
        }
        .form-control:focus {
            background-color: #495057; /* Maintain dark background on focus */
            color: #e9ecef; /* Maintain light text color on focus */
            border-color: #80bdff; /* Blue border color on focus */
            box-shadow: 0 0 0 0.2rem rgba(38, 143, 255, 0.25); /* Blue shadow on focus */
        }
        .btn-primary {
            background-color: #007bff; /* Primary button background color */
            border-color: #007bff; /* Primary button border color */
        }
        .btn-secondary {
            background-color: #6c757d; /* Secondary button background color */
            border-color: #6c757d; /* Secondary button border color */
        }
        .btn-container {
            display: flex;
            gap: 1rem; /* Space between buttons */
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <h1 class="mb-4">Update Book</h1>
        <form id="updateBookForm" action="/library/update-book/<%= id %>" method="POST">
            <input type="hidden" id="id" name="id" value="<%= id %>">
            
            <div class="mb-3">
                <label for="title" class="form-label">Title:</label>
                <input type="text" id="title" name="title" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="author" class="form-label">Author:</label>
                <input type="text" id="author" name="author" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="date" class="form-label">Publication Date:</label>
                <input type="date" id="date" name="publishedDate" class="form-control" required>
            </div>
            <div class="btn-container mt-3">
                <button type="submit" class="btn btn-primary">Update Book</button>
                <a href="/library/read-book" class="btn btn-secondary">Back to All Books</a>
            </div>
        </form>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>
