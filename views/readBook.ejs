<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= book.title %> - Book Details</title>

    <!-- Bootstrap CSS from CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Custom CSS for dark theme -->
    <style>
        body {
            background-color: #212529; /* Dark background for the body */
            color: #e9ecef; /* Light text color */
        }
        .read-book-container {
            background-color: #343a40; /* Dark background for the container */
            padding: 2rem;
            border-radius: 0.5rem;
            max-width: 600px;
            margin: auto;
        }
        .read-book-title {
            margin-bottom: 1rem;
            color: #e9ecef; /* Light text color for the title */
        }
        .read-book-date {
            margin-bottom: 1.5rem;
            color: #adb5bd; /* Lighter text color for the date */
        }
        .button-group {
            display: flex;
            gap: 1rem; /* Space between buttons */
            margin-bottom: 1.5rem;
        }
        .btn-primary {
            background-color: #007bff; /* Primary button background color */
            border-color: #007bff; /* Primary button border color */
        }
        .btn-secondary {
            background-color: #6c757d; /* Secondary button background color */
            border-color: #6c757d; /* Secondary button border color */
        }
        .btn-danger {
            background-color: #9e4646; /* Danger button background color */
            border-color: #6c757d; /* Border color for danger button */
            color: #c6ccd1; /* Text color for better visibility */
        }
        .btn:hover {
            transform: scale(1.05); /* Slightly increase button size */
            transition: transform 0.2s ease-in-out; /* Smooth transition effect */
        }
    </style>
</head>

<body>
    <div class="read-book-container mt-5">
        <h1 class="read-book-title"><%= book.title %> by <%= book.author %></h1>
        <p class="read-book-date">Published on: <%= book.formattedDate %></p>

        <!-- Buttons with custom styles -->
        <div class="button-group">
            <a href="/library/update-book/<%= book.id %>" class="btn btn-secondary">Edit</a>
            <form action="/library/delete-book/<%= book.id %>" method="POST" class="inline-form" onsubmit="return confirmDelete(event)">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
        </div>

        <a href="/library/read-book" class="btn btn-primary">Back to All Books</a>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    
    <script>
        function confirmDelete(event) {
            event.preventDefault(); // Prevent the default form submission

            if (confirm('Are you sure you want to delete this book?')) {
                // Submit the form if confirmed
                event.target.submit();
            }

            return false; // Prevent default form submission
        }
    </script>
</body>

</html>
